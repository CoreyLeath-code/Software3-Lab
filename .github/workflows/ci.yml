name: .NET CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: dotnet-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x
          cache: true
          cache-dependency-path: "**/*.csproj"

      - name: Restore
        run: dotnet restore Software3-Lab.sln

      - name: Build
        run: dotnet build Software3-Lab.sln --configuration Release --no-restore

      - name: Test
        run: >
          dotnet test Software3-Lab.sln
          --configuration Release
          --no-build
          --logger "trx;LogFileName=test-results.trx"
          --collect:"XPlat Code Coverage"

      - name: Benchmark
        run: >
          dotnet run
          --project benchmarks/LoanTracker.Benchmarks.csproj
          --configuration Release
          --no-build
          --
          --filter "*"

      - name: Upload test and benchmark results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dotnet-quality-results
          path: |
            tests/TestResults/**
            BenchmarkDotNet.Artifacts/results/**
          if-no-files-found: warn
